// import { createSelector } from 'reselect';
// import { TRANSMITTER_PAGE_STORE_KEY } from './transmitter-page-constants';

// const transmitterPageState = state => state[TRANSMITTER_PAGE_STORE_KEY];

// export const getCurrentPositionSelector = createSelector(
//   transmitterPageState,
//   (state) => {
//     return state.currentCoord;
//   },
// );
